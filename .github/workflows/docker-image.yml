on:
  push:
    tags:
      - v1.**
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to JFrog  
        uses: docker/login-action@v1
        with:
          registry: sdai.jfrog.io
          username: isparkes@sda-institute.com
          password: Qg89TU2^xL
      -
        name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: sdai.jfrog.io/${{env.GITHUB_REF#refs/*/}}:latest
